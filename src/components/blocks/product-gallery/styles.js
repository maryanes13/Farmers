import styled from "styled-components";
import { Swiper } from "swiper/react";

export const StyledSection = styled(Swiper)`
  width: 727px;
  height: 768px;
  .swiper-pagination {
    display: none;
  }
`;
